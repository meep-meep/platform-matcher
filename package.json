{
  "name": "mm-platform-matcher",
  "version": "0.2.0",
  "description": "matches platform constraints against a user agent object",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha --check-leaks -R dot",
    "cov": "mocha --require blanket -R html-cov > coverage.html"
  },
  "keywords": [
    "test"
  ],
  "author": "Joachim Revol-Tissot",
  "license": "MIT",
  "dependencies": {
    "express-useragent": "^0.1.6"
  },
  "devDependencies": {
    "blanket": "^1.1.7",
    "expect.js": "^0.3.1",
    "mocha": "^2.3.3",
    "mockery": "^1.4.0"
  }
}
